apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-redhat-kb.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-redhat-kb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcp-redhat-kb.selectorLabels" . | nindent 4 }}
